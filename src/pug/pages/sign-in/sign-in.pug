extends ../../layout/main
include ../../utils/mixins

block variables
  - const title = 'Sign in';
  - const pageClass = '';
  - const footer = require('../../../components/footer/footer.json');
  - const navigations = require('../../../components/nav/nav.json');

block content
  +header({navigations, status: 'non-authorized'})
  .main
    section.sign-in
      .sign-in__container
        +signIn
  +footer(footer)