block variables
include ../utils/mixins

- const header = require('../../components/header/header.json');
- const footer = require('../../components/footer/footer.json');

doctype html
html(lang="ru")
  +head
  body(class=pageClass)
    .burger
      span
    .site-container
      +header({status: 'non-authorized', navigations: header})
      .main
        block content
      +footer(footer)