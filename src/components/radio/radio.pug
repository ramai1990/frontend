mixin radio({name, items})
  .radio
    each item in items
      - const { checked=false, value, text } = item;
        label.radio__label
          input.radio__input(type="radio" name=name value=value checked=checked)
          span.radio__label-text= text