mixin card(config)
  -
    const {
      number = 001,
      text = '',
      price = '9 990₽',
      counter = 100,
      slides = [{image: 'luxe.jpg'}],
      rating = 'five'
    } = config;
  .card
    +swiper({ slides })
    .card__title
      a(href='https://ramai1990.github.io/frontend/room').card__title-left
        span.card__title-left-number №
        | #{number}
        span.card__title-left-text= text
      p.card__title-right= price
        span.card__title-right-text в сутки
    .card__reviews
      +rating(rating)
      p.card__text= counter
        span.card__text-feedback Отзывов