mixin rating(items)
  each item in items
    - const { classes, selectClass } = item;
    .rate-elements__item(class=classes)
      select(class=selectClass)
        option(value='1')
        option(value='2')
        option(value='3')
        option(value='4')
        option(value='5')