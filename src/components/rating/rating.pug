mixin rating(items)
  each item in items
    - const { classes, rating } = item;
    .rate-elements__item(class=classes)
      select(class=`rate-elements__rating-${rating}`)
        each value in [1,2,3,4,5]
          option(value=`${value}`)= value